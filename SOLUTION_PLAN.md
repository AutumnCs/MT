# 现在就出发：AI 本地路线智能规划

## 1. 赛题目标理解

### 1.1 任务本质

这道题不是单纯做一个“地图导航”工具，而是做一个 **面向本地出行/游玩场景的多目标决策助手**。

用户的真实困难是：

1. 目的地不是单点，而是多个 POI 的组合
2. 约束很多，而且常常互相冲突  
   例如：想吃得好、又不想排队、预算有限、时间有限、最好顺路
3. 用户往往只能给出自然语言需求，而不是结构化筛选条件
4. 用户希望拿到的是 **可执行的完整方案**，而不是一堆 POI 列表

所以项目的核心价值不是“推荐一个店”，而是：

**把用户自然语言需求，转成一条满足时间、预算、偏好、路线效率约束的可执行路线方案。**

---

## 2. 题目要求拆解

根据题面和答疑信息，项目需要满足下面四件事：

### 2.1 完成端到端流程

必须形成闭环：

1. 用户输入游玩 / 出行目标
2. 系统解析目标和约束
3. 结合 POI 数据与服务信息
4. 自动生成多维度路线方案
5. 支持用户继续调整条件，再次规划

### 2.2 满足交付目标

#### 路线生成

- 能根据用户意图自动串联多个 POI
- 生成完整路线安排

#### 多条件与个性化

- 能处理差异化约束
- 结合用户偏好生成个性化方案

### 2.3 对齐评分关注点

根据答疑截图，评委重点看三块：

#### 完整性

- 路线是否真实可执行
- 时间安排是否合理
- POI 覆盖是否准确
- 是否满足多约束条件

#### 创新性

- 是否支持自然语言实时调整
- 是否支持反馈闭环
- 是否支持多方案比较
- 是否体现 LLM + 搜索/规划融合

#### 应用效果

- 页面是否能跑起来
- 交互是否顺畅
- 工程结构是否清晰
- 文档和方案是否完整

### 2.4 满足显式约束

从截图里能提取出三条硬约束：

1. 路线生成时间 `< 10 秒`
2. POI 类型至少覆盖两类，例如：
   - 餐饮
   - 娱乐 / 文化
3. 路线规模支持 `>= 3 个 POI` 串联

---

## 3. 我们的产品定位

### 3.1 产品名称

**现在就出发：AI 本地路线智能规划**

### 3.2 产品定位

一个面向本地出行、约会、朋友来访、周末闲逛等场景的 AI 路线规划助手。

### 3.3 产品主张

用户不是来“搜店”的，而是来“拿方案”的。  
我们要提供的是：

**一句话输入需求 -> 自动生成一条可执行路线 -> 用户继续微调 -> 系统动态重规划**

### 3.4 典型使用场景

1. 约会场景  
   “周六下午从广州塔出发，预算 200，想喝咖啡、看展、吃饭，不想太累，9 点前结束。”

2. 下班休闲场景  
   “今晚下班后想吃点好的再散步，预算 180，不想排太久队。”

3. 朋友来访场景  
   “周末带朋友逛广州，想拍照、喝咖啡，顺便吃饭，路线不要太绕。”

---

## 4. 方案设计

## 4.1 总体架构

当前项目建议采用：

- **前端**：Flutter
- **后端**：FastAPI
- **规划核心**：规则约束 + 打分排序 + 路线拼接
- **数据层**：本地 POI JSON + 可扩展到真实 POI / UGC / 服务数据

整体链路：

```text
用户自然语言输入
  -> 意图解析
  -> 约束抽取
  -> POI 检索
  -> 约束过滤
  -> 路线生成
  -> 结果解释与展示
  -> 用户二次修改
  -> 动态重规划
```

---

## 4.2 后端模块设计

结合你现在已有代码，后端已经有一条基础链路：

### 1. `intent_parser.py`

职责：

- 从自然语言中提取
  - 城市
  - 出发地
  - 时间
  - 预算
  - 类别需求
  - 偏好
  - 回避项

当前作用：

- 这是“自然语言 -> 结构化意图”的第一层

后续建议增强：

- 修复当前文本乱码问题
- 增加更多场景词典
- 增加时间段、人数、交通方式、排队容忍度识别

### 2. `poi_retriever.py`

职责：

- 从 `pois.json` 中取出候选 POI

当前作用：

- 按城市、类别做基础筛选

后续建议增强：

- 增加评分、价格、营业时段、热门时段等字段
- 为每个 POI 增加“适合约会/拍照/轻松/夜间”等标签

### 3. `constraint_checker.py`

职责：

- 根据预算、偏好、避雷条件过滤 POI

当前作用：

- 这是“可用性控制层”

后续建议增强：

- 加入时间冲突过滤
- 加入距离约束过滤
- 加入排队时间模拟过滤

### 4. `route_planner.py`

职责：

- 把筛选后的 POI 串成路线

当前作用：

- 这是核心规划模块

后续建议增强：

- 输出不止一条路线，而是 2~3 条不同目标路线：
  - 省时路线
  - 高评分路线
  - 性价比路线

### 5. `response_generator.py`

职责：

- 组装前端展示用的最终结果

当前作用：

- 把路线结果转成结构化响应

后续建议增强：

- 为每个 POI 生成推荐理由
- 输出“为什么这条路线适合你”
- 输出可调整建议

### 6. `main.py`

当前接口：

- `POST /api/route/generate`
- `POST /api/route/modify`

这已经很适合比赛演示，因为它天然支持：

1. 首次生成路线
2. 用户反馈后动态修改路线

---

## 4.3 前端产品设计

前端现在建议围绕“轻量闭环”做，不要一下子铺太多页。

### 页面 1：需求输入页

目标：

- 让用户用一句自然语言说清需求

核心元素：

- 多行输入框
- 示例需求卡片
- 生成路线按钮

### 页面 2：路线结果页

目标：

- 展示完整的多 POI 路线方案

核心元素：

- 路线标题
- 路线摘要
- POI 列表
- 每个站点的推荐理由
- 预算 / 时间 / 类型概览

### 页面 3：二次修改交互

目标：

- 支持用户继续说一句话微调路线

示例：

- “太远了，换近一点的”
- “不想吃辣”
- “再加一个甜品店”
- “预算降到 150”

### 页面 4：多方案对比（可选增强）

目标：

- 体现创新性和应用效果

可对比三条路线：

- 最省时
- 最适合约会
- 性价比最高

---

## 5. 数据设计

## 5.1 当前可用数据

当前项目中已经有：

- `backend/pois.json`

比赛第一阶段完全可以先用这份本地数据跑闭环。

## 5.2 建议补充字段

每个 POI 建议至少具备：

- `name`
- `city`
- `category`
- `rating`
- `price`
- `estimated_duration`
- `distance`
- `tags`
- `open_hours`
- `queue_level`
- `photo_score`
- `romantic_score`
- `night_friendly`

这样可以明显提高路线质量和解释能力。

---

## 6. 路线生成策略

## 6.1 最小可用策略

为了保证比赛演示稳定，建议先用：

**规则约束 + 打分排序 + 顺序拼接**

步骤：

1. 抽取意图
2. 按类别召回 POI
3. 用预算、偏好、回避项过滤
4. 对候选 POI 打分
5. 按目标函数生成路线

### 示例打分维度

- 评分高
- 价格合适
- 标签匹配
- 距离更近
- 时间更合理
- 排队更短

## 6.2 比赛版推荐输出

建议至少输出 2 条方案：

1. **稳妥方案**  
   最强调可执行性、顺路、低风险

2. **偏好方案**  
   更强调拍照、约会、氛围、体验

如果时间够，再加：

3. **高性价比方案**

---

## 7. 创新点设计

为了对齐“创新性”，建议明确强调下面 4 个点：

### 7.1 自然语言即输入

不要求用户手动点选很多筛选器，一句话就能开始规划。

### 7.2 多目标规划

不是单点推荐，而是多 POI 串联路线规划。

### 7.3 动态调整

用户可以实时反馈，系统重新生成路线，而不是回到头重选。

### 7.4 推荐理由解释

不仅给结果，还解释：

- 为什么选这些点
- 为什么顺序这样排
- 哪些约束被优先满足

---

## 8. 当前项目现状与差距

## 8.1 现在已有的

1. 后端已有基础 API 闭环
2. 已有意图解析、POI 检索、约束过滤、路线生成模块
3. Flutter 前端工程已搭好
4. Android 模拟器与 Flutter 调试链路已打通

## 8.2 当前明显缺口

1. **中文文本存在乱码**
   - 后端部分 Python 文件仍有乱码
   - Flutter 首页也有乱码文本

2. **前端还没有真正接后端**
   - 当前更多是页面壳子

3. **路线结果页还不完整**
   - 还缺路线详情、POI 卡片、推荐理由、二次修改

4. **规划逻辑还偏基础**
   - 还没有多方案输出
   - 还没有时间/距离/排队等更细的综合打分

---

## 9. 建议开发优先级

建议按“先闭环、再增强”的顺序来。

### 第一阶段：先把最小闭环跑通

目标：

- 可以完整演示

要完成：

1. 修复前后端中文乱码
2. Flutter 首页接 `generate` 接口
3. Flutter 结果页接结果展示
4. 能看到至少 3 个 POI 串联的路线
5. 能通过一句话再次修改路线

### 第二阶段：补比赛亮点

目标：

- 提升创新性和应用效果

要完成：

1. 多方案输出
2. 推荐理由解释
3. 预算 / 时间 / 类型摘要
4. 示例需求一键填充
5. 更完整的视觉呈现

### 第三阶段：准备答辩材料

目标：

- 让评委快速理解你们的价值

要准备：

1. 任务背景
2. 用户痛点
3. 系统架构图
4. 端到端流程图
5. 结果页截图
6. 与普通 POI 搜索的差异

---

## 10. 推荐答辩话术主线

可以围绕下面这条线讲：

### 10.1 问题定义

用户在本地出行和游玩时，真正痛的不是“找不到店”，而是“不会把多个点高效串起来”。

### 10.2 我们的方案

我们做的是一个 AI 本地路线规划助手，能理解自然语言需求，把多个 POI 自动串联成可执行路线。

### 10.3 技术路径

系统把用户输入解析成结构化约束，结合 POI 数据、偏好标签和规则约束，自动生成路线，并支持用户二次反馈后动态调整。

### 10.4 结果价值

相比传统搜索和推荐，本方案把“反复筛选 + 自己拼路线”的高决策成本，变成“一句话得到完整可执行方案”。

---

## 11. 推荐里程碑

### 里程碑 A：本周先完成

- Flutter 首页接后端
- 结果页可展示路线
- 后端中文修复

### 里程碑 B：再补

- 路线修改接口联调
- 多方案支持
- 推荐理由输出

### 里程碑 C：答辩前

- 页面细节打磨
- 文档整理
- 演示录屏 / 截图

---

## 12. 一句话版本总结

**我们要做的不是“推荐几个点”，而是把用户一句自然语言需求，转成一条满足时间、预算、偏好和路线效率约束的可执行本地路线方案。**

