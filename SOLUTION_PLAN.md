# 现在就出发：AI 本地路线智能规划

## 1. 问题定义

用户在出行、约会、游玩、临时安排时，通常会遇到这些问题：

- 目的地不是单点，而是多个 POI 的组合
- 预算、时间、排队、距离、偏好等约束经常互相冲突
- 用户表达需求时通常是自然语言，而不是结构化条件
- 用户真正想要的是一条可直接执行的路线，而不是一堆推荐点

因此，这个项目要解决的核心问题不是“推荐一个店”，而是：

> 把用户自然语言需求，转成一条满足时间、预算、偏好和路线顺序约束的可执行路线。

---

## 2. 项目目标

### 2.1 端到端闭环

项目需要完成以下链路：

1. 用户输入出行 / 游玩需求
2. 系统解析需求和约束
3. 结合 POI 数据与服务信息检索候选点
4. 对候选点进行多维评分和排序
5. 生成完整路线方案
6. 支持用户继续修改条件并重新规划

### 2.2 满足交付目标

#### 路线生成

- 能根据用户意图自动串联多个 POI
- 生成完整路线安排

#### 多条件与个性化

- 能处理差异化约束
- 能结合用户偏好生成个性化方案

#### 动态调整

- 用户可以在结果页继续输入修改意见
- 系统可以重新规划路线

---

## 3. 评分重点对齐

### 3.1 完整性

- 路线是否真实可执行
- 时间安排是否合理
- POI 覆盖是否准确

### 3.2 创新性

- 是否使用大模型理解自然语言意图
- 是否支持路线重规划
- 是否支持城市切换和偏好驱动规划

### 3.3 应用效果

- 页面是否完整
- 系统是否能稳定演示
- 结果是否有可解释性

---

## 4. 总体方案

项目采用“LLM + 规则 + 排序 + 路线调度”的分层架构：

### 4.1 LLM 负责理解

LLM 的职责是把用户自然语言转成结构化意图，包括：

- 城市
- 起点
- 预算
- 起止时间
- 偏好
- 避雷项
- 节奏
- 交通方式

当前已通过 `backend/llm_intent_client.py` 接入 DashScope / OpenAI。无 key 或调用失败时，系统自动回退到 `intent_parser.py` 的规则解析，保证演示稳定。

### 4.2 规则负责约束

规则层只负责硬约束：

- 城市范围
- 预算上限
- 时间窗口
- 营业时间
- 明确避雷项

不再在这里做复杂语义判断。

### 4.3 排序负责偏好

POI 排序模块根据偏好、标签、评分、预算适配、排队情况、模拟评论信号等进行多维打分。

当前新增 `review_analyzer.py`，用于从以下字段中提取 UGC 风格信号：

- 标签
- 适合人群
- 描述
- 评论关键词
- 正向评论
- 负向评论
- 显式 `review_signals`

### 4.4 路线调度负责执行

路线规划模块负责：

- 站点顺序
- 停留时长
- 交通时间
- 总时长控制
- 分时段适配
- 转场占比控制
- 多候选方案比较
- 执行风险提醒
- 路线说明生成

---

## 5. 当前实现形态

### 5.1 前端

Flutter 页面已具备：

- 需求输入页
- 路线结果页
- 修改路线入口
- 城市切换
- 偏好 chips

### 5.2 后端

FastAPI 后端已具备：

- LLM 优先、规则兜底的意图解析
- 意图解析
- 硬约束校验
- POI 检索
- UGC 信号模拟分析
- POI 排序
- 路线规划与多方案生成
- 结果生成

### 5.3 数据

POI 数据已支持广州 / 上海双城结构，并扩展了多维评分字段：

- 区域
- 子类别
- 营业时间
- 推荐时长
- 排队等级
- 拍照分
- 约会分
- 美食分
- 文化分
- 本地特色分
- 雨天适配分
- 商圈
- 最佳访问时段
- 评论关键词
- 正负向模拟评论
- 分时排队 profile
- 显式评论信号

当前还没有真实点评数据，因此使用本地模拟数据承载 UGC 智慧。后续可以通过脚本批量生成更大规模的模拟 POI 和评论。

### 5.4 前端展示

Flutter 结果页已展示：

- 路线摘要
- 路线评分
- 转场占比
- 候选方案
- 执行提醒
- 站点时间线
- 单站推荐理由和风险

---

## 6. 下一步建议

### 6.1 最优先

- 扩充广州 / 上海模拟 POI 数量
- 增加模拟评论语料，填充 `review_keywords`、`positive_reviews`、`negative_reviews`
- 给 POI 增加 `business_area` 和 `best_visit_periods`

### 6.2 第二优先

- 让 modify 真正影响路线结果
- 让用户说“太远了”“预算低一点”“别排队”时，路线能变化
- 支持局部替换站点，而不是完全重算

### 6.3 第三优先

- 增加更直观的地图感展示
- 用真实地图 API 或离线 travel matrix 替代当前经纬度估算

---

## 7. 总结

这个项目的本质是：

> 面向本地出行与游玩场景，把用户的自然语言需求转成可执行路线方案。

当前最合理的路线是：

**LLM 负责理解，规则负责约束，排序负责偏好，路线规划负责执行。**

