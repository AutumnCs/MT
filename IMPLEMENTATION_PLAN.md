# MeituanAgent 项目实施方案

## 1. 项目一句话定义

**把用户一句自然语言需求，转成一条满足时间、预算、偏好和路线效率约束的本地多 POI 可执行路线。**

---

## 2. 赛题要求转成产品目标

根据题面和答疑，项目至少要证明这四件事：

1. **能理解自然语言需求**
   - 用户不用点很多筛选器
   - 一句话就能输入目标和约束

2. **能把多个 POI 串成路线**
   - 不是推荐一个点
   - 而是生成完整的顺路方案

3. **能处理多约束**
   - 预算
   - 时间
   - 偏好
   - 回避条件
   - 路线效率

4. **能动态调整**
   - 用户说“太远了”
   - “预算降一点”
   - “不想吃辣”
   - 系统能重新规划

---

## 3. 评分点对应策略

## 3.1 完整性

评委会看：

- 路线是否真实可执行
- 时间是否合理
- POI 选择是否准确
- 多约束是否有体现

我们的策略：

- 至少生成 3 个 POI 的完整路线
- 每个站点有到达/离开时间
- 给出预算、距离、总时长
- 把“为什么选这个点”解释清楚

## 3.2 创新性

评委会看：

- 是否支持自然语言交互
- 是否支持动态修改
- 是否支持多方案
- 是否体现 LLM + 规划融合

我们的策略：

- 一句话生成路线
- 一句话修改路线
- 输出 2~3 条不同偏向方案
- 用“意图解析 + 约束过滤 + 路线打分 + 解释生成”体现智能性

## 3.3 应用效果

评委会看：

- 页面是否跑起来
- 交互是否顺畅
- 文档是否完整
- 工程结构是否清晰

我们的策略：

- Flutter 前端保证移动端体验
- FastAPI 后端保证演示稳定
- 提供 README / COMMANDS / 方案文档
- 保持前后端职责清晰

---

## 4. 当前技术方案

## 4.1 前端

- **框架**：Flutter
- **目标**：移动端演示体验稳定、热重载开发效率高

### 前端建议页面

1. **需求输入页**
   - 输入自然语言需求
   - 点击生成路线
   - 示例需求快捷填充

2. **路线结果页**
   - 展示路线标题、摘要、预算、时长
   - 展示多个 POI 站点
   - 每个站点展示推荐理由

3. **动态修改交互**
   - 输入“太远了”“换一家甜品店”“预算 150”
   - 调后端 `modify` 接口重新生成

## 4.2 后端

- **框架**：FastAPI
- **当前接口**
  - `POST /api/route/generate`
  - `POST /api/route/modify`

### 后端模块职责

1. `intent_parser.py`
   - 解析城市、预算、时间、偏好、回避条件

2. `poi_retriever.py`
   - 从 POI 数据中召回候选点

3. `constraint_checker.py`
   - 进行预算、偏好、风险过滤

4. `route_planner.py`
   - 串联路线并生成顺序

5. `response_generator.py`
   - 组装前端展示数据

---

## 5. 推荐系统流程

```text
用户输入自然语言
-> 意图解析
-> POI 召回
-> 约束过滤
-> 路线打分与排序
-> 生成路线
-> 输出解释
-> 用户继续反馈
-> 动态重规划
```

---

## 6. 核心能力设计

## 6.1 输入理解

输入示例：

- “周六下午从广州塔出发，预算 200，想喝咖啡、看展、吃饭，不想太累，9 点前结束”
- “今晚下班后想吃点好的再散步，预算 180，不想排太久队”
- “周末带朋友逛广州，想拍照、喝咖啡，顺便吃饭，路线不要太绕”

系统要识别出：

- 城市
- 起点
- 时间段
- 预算
- 类别需求
- 风格偏好
- 避雷条件

## 6.2 路线生成

第一阶段采用：

**规则约束 + 打分排序 + 顺路拼接**

打分维度建议：

- 评分
- 价格
- 标签匹配
- 距离
- 停留时长
- 排队风险

## 6.3 多方案输出

建议至少输出 2 条方案：

1. **稳妥方案**
   - 更顺路
   - 更省时
   - 风险更低

2. **体验方案**
   - 更强调氛围、拍照、约会体验

如果时间足够，再补：

3. **性价比方案**

## 6.4 动态修改

用户二次输入后，不需要重来，而是走：

`modify -> 重新解析 -> 重新过滤 -> 重新排序 -> 新路线输出`

---

## 7. 当前项目最应该补的东西

## 7.1 先补最小闭环

优先级最高：

1. 修复后端中文乱码
2. Flutter 首页接 `generate` 接口
3. Flutter 结果页展示路线数据
4. 接 `modify` 接口支持二次修改

## 7.2 再补比赛亮点

第二优先级：

1. 多方案输出
2. 推荐理由解释
3. 风险提醒
4. 路线摘要卡

## 7.3 最后补答辩材料

第三优先级：

1. 流程图
2. 架构图
3. 页面截图
4. 演示稿

---

## 8. 开发里程碑

## 里程碑 A：能跑

- 后端接口稳定
- Flutter 首页可调用接口
- 结果页可展示路线

## 里程碑 B：能讲

- 有推荐理由
- 有多条件约束体现
- 有动态修改

## 里程碑 C：能打动评委

- 有多方案对比
- 有清晰视觉呈现
- 有稳定演示流程

---

## 9. 后端部署建议

这个问题分阶段看，不是只有“本地”或“云服务器”两个极端。

## 9.1 开发阶段

**建议：后端跑本地**

原因：

- 改代码快
- 联调最方便
- 不需要先折腾运维
- 你现在重点是先把产品闭环做出来

适合方式：

- FastAPI 直接跑在你电脑上
- Flutter 模拟器直接请求本机后端

## 9.2 本地演示 / 课堂汇报 / 内部排练

**建议：后端也可以继续本地跑**

如果：

- 演示设备和电脑在同一网络
- 或者你直接用安卓模拟器

这时完全没必要为了“看起来更正式”过早上云。

## 9.3 正式答辩 / 给评委现场演示

**建议：答辩前准备一个云服务器版本**

原因：

- 不依赖你本机网络波动
- 不依赖局域网 IP
- 不怕现场换网络后接口地址失效
- 更适合多人同时访问

推荐方式：

- 一台轻量云服务器即可
- 部署 FastAPI
- 用 `uvicorn + nginx` 或简单容器方式都可以

## 9.4 结论

### 当前阶段

**先本地跑就行。**

### 答辩前

**建议补一个云服务器版本作为稳定兜底。**

最推荐的节奏是：

1. 现在先本地开发和联调
2. 功能闭环稳定后
3. 再把后端部署到云上

也就是说：

**现在不必为了部署打断开发；但正式答辩前，最好有一个云端版本。**

---

## 10. 推荐答辩主线

可以按这条线讲：

1. 用户的问题不是“找不到店”，而是“不会把多个点高效串起来”
2. 我们做的是 AI 本地路线规划助手
3. 用户一句话输入需求，系统自动生成完整可执行方案
4. 系统还能根据反馈动态重规划
5. 相比传统 POI 搜索，决策成本更低、执行性更强

---

## 11. 现在最合理的执行顺序

### 第一步

先把后端中文问题修干净

### 第二步

把 Flutter 首页接上 `generate` 接口

### 第三步

补结果页和修改交互

### 第四步

补多方案和推荐理由

### 第五步

准备答辩展示和云端部署

