# MeituanAgent 项目实施方案

## 1. 项目一句话定义

把用户一句自然语言需求，转成一条满足时间、预算、偏好和路线效率约束的本地多 POI 可执行路线。

---

## 2. 题目要求转成产品目标

项目至少要证明四件事：

1. 能理解自然语言需求
2. 能串联多个 POI 形成路线
3. 能处理预算、时间、偏好、避雷等多约束
4. 能支持用户二次反馈后重新规划

---

## 3. 评分重点对应策略

### 3.1 完整性

评委会看：

- 路线是否真实可执行
- 时间是否合理
- POI 是否覆盖得当
- 是否满足多约束

我们的策略：

- 至少生成 3 个 POI 的完整路线
- 每个站点给出到达/离开时间
- 给出预算、总时长、总距离
- 给出“为什么这样排”的解释

### 3.2 创新性

评委会看：

- 是否支持自然语言交互
- 是否支持动态修改
- 是否体现 LLM + 规则 + 排序融合

我们的策略：

- 一句话生成路线
- 一句话修改路线
- 多偏好软排序
- 城市切换支持广州 / 上海

### 3.3 应用效果

评委会看：

- 页面是否能跑起来
- 交互是否顺畅
- 文档是否完整
- 工程结构是否清晰

我们的策略：

- Flutter 前端保证移动端体验
- FastAPI 后端保证演示稳定
- README / COMMANDS / 方案文档齐全

---

## 4. 总体技术方案

### 4.1 前端

- Flutter
- 负责需求输入、结果展示、二次修改、城市切换

### 4.2 后端

- FastAPI
- 负责意图解析、POI 检索、约束检查、排序、路线生成、结果解释

### 4.3 LLM 层

- 负责意图理解与结果解释
- 不直接决定路线
- 不直接做硬过滤

### 4.4 规则层

- 负责预算、时间、营业时间、避雷项等硬约束
- 不负责主观偏好硬筛

### 4.5 数据层

- 当前使用本地 POI JSON 数据
- 可逐步扩展为真实 POI / 评论 / 服务信息

---

## 5. 后端模块划分

### `prompt_templates.py`

作用：

- 约束 LLM 的 JSON 输出格式
- 明确只做理解，不做路线规划

### `intent_parser.py`

作用：

- 归一化 LLM 草案
- 处理城市、时间、预算、起点、偏好兜底

### `constraint_checker.py`

作用：

- 检查硬约束是否可行
- 预算、时间、避雷、城市范围、营业时间

### `poi_retriever.py`

作用：

- 从 POI 数据中召回候选点

### `poi_ranker.py`

作用：

- 多维评分
- 偏好软加权
- 推荐理由生成

### `route_planner.py`

作用：

- 把候选 POI 串成路线
- 计算停留时长、交通时间、总时长

### `response_generator.py`

作用：

- 把结构化结果包装成自然语言解释

### `main.py`

当前接口：

- `POST /api/route/generate`
- `POST /api/route/modify`
- `POST /api/intent/parse`
- `POST /api/intent/prompt`

---

## 6. 路线生成流程

```text
用户自然语言输入
  -> LLM 意图解析
  -> 硬约束校验
  -> POI 召回
  -> 偏好排序
  -> 路线调度
  -> 解释生成
  -> 前端展示
  -> 用户二次修改
  -> 动态重规划
```

---

## 7. 当前的产品形态

### 首页

- 需求输入
- 城市切换
- 偏好 chips
- 示例输入

### 结果页

- 路线摘要
- 站点列表
- 时间轴
- 推荐理由
- 风险提醒
- 二次修改

### 计划中的增强

- 多方案输出
- 地图展示
- 更强的 modify
- 更强的 LLM 解释

---

## 8. 开发优先级

### 第一优先级

- 跑通真实闭环
- 首页 -> 后端 -> 结果页

### 第二优先级

- 做强 modify
- 做多方案

### 第三优先级

- 接入更强的 LLM
- 做地图展示

### 第四优先级

- 云端部署
- 答辩材料整理

---

## 9. 部署建议

### 开发阶段

- 后端本地跑最合适
- Flutter 模拟器直接联调

### 演示阶段

- 建议准备一份云端后端作为兜底

---

## 10. 结论

这套方案的核心是：

- LLM 负责理解和解释
- 规则负责边界和可行性
- 排序负责偏好和体验
- 调度负责把 POI 变成能走的路线

