# 实施总结：从规则推荐到智能路线规划

## 1. 核心理念

项目的目标不是做一个简单的 POI 推荐器，而是做一个能够理解自然语言、生成可执行路线、并支持二次修改的路线规划系统。

最终采用的分层思路是：

- LLM 负责理解
- 规则负责边界和约束
- UGC 信号负责模拟用户评价智慧
- 排序负责偏好匹配
- 路线规划负责时空调度和多方案选择

---

## 2. 当前核心能力

### 2.1 POI 字段设计

我们扩充了 POI 数据字段，让每个点不仅有名称和分类，还具备更强的路线规划能力。

新增字段包括：

- `district`
- `business_area`
- `sub_category`
- `business_hours`
- `best_visit_periods`
- `visit_duration`
- `review_keywords`
- `positive_reviews`
- `negative_reviews`
- `queue_profile`
- `review_signals`
- `queue_level`
- `photo_score`
- `date_score`
- `food_score`
- `culture_score`
- `local_feature_score`
- `rainy_day_score`
- `indoor_outdoor`

这些字段让路线规划从“筛选 POI”升级为“组织路线”。

### 2.2 LLM 意图解析

新增 `backend/llm_intent_client.py`，支持 DashScope / OpenAI 的 OpenAI-compatible Chat Completions。

运行逻辑：

1. 优先读取本地 `.env`
2. 有 `DASHSCOPE_API_KEY` 或 `OPENAI_API_KEY` 时调用大模型
3. 大模型输出结构化 JSON
4. 后端归一化成 `ParsedIntent`
5. 失败或无 key 时自动回退到规则解析

这让系统具备更强的自然语言理解能力，同时不会牺牲演示稳定性。

### 2.3 UGC / 评论信号模拟层

新增 `backend/review_analyzer.py`，用于从模拟评论、标签、描述、适合人群中推导体验信号。

当前支持的信号包括：

- `photo`
- `date`
- `food`
- `culture`
- `local_feature`
- `rainy_day`
- `quiet`
- `queue_risk`
- `crowd_risk`
- `value`

这一步对应题目中的“结合点评 POI 数据、UGC 智慧和用户个性偏好”。目前没有真实点评数据，因此先用本地模拟字段承载和推导。

### 2.4 POI 评分模块

`backend/poi_ranker.py` 用于对候选 POI 做多维打分。

评分逻辑综合考虑：

- 偏好匹配
- 语义匹配
- 类别匹配
- 评分质量
- 预算适配
- 时段适配
- 排队惩罚
- 拥挤惩罚
- 评论/标签语义信号

这一步的意义在于：

- 不是简单过滤
- 而是让系统能解释“为什么推荐这个点”

### 2.5 偏好解析增强

`backend/intent_parser.py` 负责把用户表达转成结构化意图。

支持的内容包括：

- 城市识别
- 地标推断城市
- 时间解析
- 预算解析
- 起点解析
- 偏好归一化
- 避雷项识别
- 节奏识别

同时，我们把很多“软偏好”从硬规则里移走，避免语义规则越堆越碎。

### 2.6 路线调度逻辑

`backend/route_planner.py` 负责把排序后的 POI 串成可执行路线。

它承担的任务包括：

- 停留时长估计
- Haversine 经纬度距离估算
- 步行 / 打车 / 地铁绕行系数
- 餐饮、咖啡、展览、夜景等类别的分时段适配
- 总时长控制
- 超时裁剪
- 交通占比计算
- 路线风险提醒
- 多候选路线生成
- 路线说明生成

路线规划从“点的列表”变成“可执行行程”。

### 2.7 多方案输出

后端现在会生成三类候选方案：

- `balanced`：综合平衡
- `preference_focused`：偏好优先
- `compact`：少绕路

返回字段包括：

- `route_score`
- `travel_time_ratio`
- `warnings`
- `route_options`

前端结果页已经展示候选方案、路线评分、转场占比和执行提醒。

---

## 3. 广州 / 上海 双城支持

系统支持广州和上海双城切换。

### 3.1 前端显式选择

用户可以在首页直接选择城市，这会作为最高优先级。

### 3.2 输入自动识别

如果用户没有显式选城市，系统会根据地标和输入内容进行识别，例如：

- 广州塔、花城广场、海心沙 -> 广州
- 外滩、武康路、豫园 -> 上海

### 3.3 数据层切分

POI 数据中每个点都带有 `city` 字段，检索和排序都会先按城市筛选，再进入后续流程。

---

## 4. LLM 与规则的分工

### 4.1 LLM 负责

- 理解自然语言
- 输出结构化意图
- 将模糊修改意见转成结构化需求

### 4.2 规则负责

- 预算上限
- 时间窗口
- 营业时间风险提醒
- 明确避雷项
- 城市范围
- 路线可行性

### 4.3 为什么要这样分

如果所有内容都靠规则，会逐渐变成补丁系统。  
如果所有内容都交给大模型，又会失去可控性。  
所以最稳的方案是：

**LLM 管理解和表达，规则管边界，算法管执行。**

---

## 5. 前后端同步情况

### 5.1 前端

Flutter 前端已完成基础页面结构：

- 输入页
- 结果页
- 修改入口
- 城市切换
- 偏好 chips

### 5.2 后端

FastAPI 后端已具备：

- LLM 优先、规则兜底的意图解析
- 意图解析
- 约束校验
- POI 检索
- UGC 信号分析
- POI 排序
- 路线规划
- 多候选方案生成
- 结果生成

### 5.3 数据模型

前后端已经建立基础字段对齐，支持：

- 城市
- 起点
- 预算
- 时间
- 偏好
- 避雷项
- 停留时长
- 交通信息
- 路线摘要
- 路线评分
- 交通占比
- 风险提醒
- 候选方案

---

## 6. 当前状态

### 已完成

- 后端结构升级
- POI 数据增强
- 排序模块拆分
- 时间调度逻辑
- LLM 真实调用接入
- UGC / 评论信号模拟
- 经纬度地理估算增强
- 多候选方案输出
- 前端候选方案和执行提醒展示
- 双城支持

### 待继续

- 更强的 modify 语义理解
- 扩充模拟 POI 和模拟评论数据
- 更直观的地图感展示
- 真实地图 API / 路网矩阵
- 答辩材料整理

---

## 7. 结论

这次改造把系统从“规则驱动的推荐列表”推进到了“可以执行的路线规划支架”。

当前系统已经具备：

- 自然语言输入
- 结构化解析
- 偏好排序
- 时间调度
- 分时段适配
- 多方案比较
- UGC 信号模拟
- 双城切换
- 路线解释
- 二次修改

下一步最值得做的是：

1. 扩充模拟数据规模
2. 让 modify 更聪明
3. 增加地图感展示或真实交通矩阵

